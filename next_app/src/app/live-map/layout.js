export const metadata = {
  title: 'Live 3D Map — Disaster Awareness',
  description: 'Real-time 3D globe visualization of global disaster alerts from GDACS and satellite monitoring networks.',
};

export default function LiveMapLayout({ children }) {
  return children;
}
