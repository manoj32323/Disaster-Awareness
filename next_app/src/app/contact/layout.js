export const metadata = {
  title: 'Contact — Disaster Awareness',
  description: 'Reach our operations center for partnership inquiries, data access, or integration support.',
};

export default function ContactLayout({ children }) {
  return children;
}
